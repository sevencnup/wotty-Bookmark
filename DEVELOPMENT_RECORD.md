# WOTTY BOOKMARK 开发记录

## 2026-09-12：优化服务器书签库虚拟滚动

- 管理后台书签列表改为固定行高虚拟滚动，仅渲染视口及上下缓冲区内的少量节点。
- 保留搜索、Logo、打开、删除和响应式布局，书签数量较大时不再一次性创建全部行 DOM。

## 2026-09-12：侧边栏快速收藏选择文件夹

- 侧边栏顶部收藏卡片支持选择服务器文件夹，收藏内容可直接归档到指定层级。

## 2026-09-12：服务端缓存服务器书签 Logo

- 管理后台按唯一网站域名批量请求 favicon，由 API 解析网页声明和标准图标路径并缓存到数据目录。
- 图标使用同源缓存接口返回，前端避免逐条探测外部服务；失败结果短期负缓存，减少刷新等待。
- 增加远程请求超时、响应大小、图片类型、重定向和内网地址限制，避免 favicon 代理成为 SSRF 入口。

## 2026-09-12：修复服务器书签网站 Logo 缺失

- 为管理后台书签列表增加多候选 favicon 地址和公共图标服务兜底，站点没有 `/favicon.ico` 时仍可显示网站 Logo。
- 保留无图标站点的首字母回退，并完成回归测试、类型检查与生产构建。

## 2026-09-12：优化服务器书签库展示模块

- 重构服务器书签列表的信息层级，增加网站图标、域名、完整网址、文件夹和独立打开操作。
- 优化长标题截断、搜索清除、列表悬停反馈和窄屏布局，保留原有删除、搜索与刷新能力。

## 2026-09-12：缓存侧边栏网站图标

- 使用浏览器 Cache Storage 按网站域名持久化已下载的 favicon，侧边栏重载后可直接复用本地图标。
- 同一域名的多个书签共享一个加载任务和缓存条目，减少重复网络请求。

## 2026-09-12：修复侧边栏顶部 Logo 尺寸

- 将顶部品牌 Logo 放入固定尺寸容器，避免原始图片尺寸撑高侧边栏标题栏。

## 2026-09-12：修复侧边栏网站图标溢出

- 固定服务器书签库中网站图标的渲染尺寸，避免网站返回大尺寸 Logo 撑高书签行。

## 2026-09-12：修复局域网 HTTP 页面复制按钮

- 为管理后台复制按钮增加 Clipboard API 被浏览器拒绝时的兼容复制回退，并仅在实际成功后提示已复制。

## 2026-09-12：自有服务器书签库模式

- 新增与 Floccus XBEL 索引完全隔离的服务器书签库、回收站和用户隔离 API。
- 侧边栏收藏、搜索、编辑、移动与删除全部改为直接操作服务器数据，并移除浏览器书签权限。
- 管理后台新增“我的书签库”，支持从当前同步库复制迁移及 XBEL 导入。

## 2026-09-12：修复开发环境备份 API 404

- 定位并重启了仍运行旧二进制的 Rust API；管理后台代理配置无需调整。
- 通过管理后台端口和 API 端口复测备份路由，均已返回正常鉴权响应。

## 2026-09-11：补齐服务器备份下载与还原闭环

- 备份支持完整归档下载、结构校验、还原前自动保护和失败回滚。
- 还原覆盖数据库、主密钥、同步文件与历史版本，并提示重启 API 服务。

## 2026-09-11：移除未实现的标签管理入口

- 盘点侧边栏后确认当前 Floccus XBEL 同步链路没有稳定的跨设备标签数据模型。
- 移除标签管理导航、页面占位组件和无用样式，避免展示无法与同步文件保持一致的虚假云端标签。
- 将旧版本保存在本地偏好设置中的标签管理默认页迁移到概览。

## 2026-09-11：管理后台自动备份

- 新增服务器级自动备份：SQLite 数据库快照、服务器主密钥、同步文件与历史版本目录。
- 支持每日指定时间、最多保留 1～100 份、立即备份和备份运行记录。
- 管理后台增加“数据备份”页面，展示计划、下一次执行时间、备份状态和错误信息。
- 备份目录固定在数据目录 backups 子目录，并排除备份目录自身，避免递归复制。

## 2026-08-30：Floccus 单一专用密码配置

- 新增事务化 Floccus 凭据接口，一次生成同一串 WebDAV 密码与加密 Passphrase。
- WebDAV 认证仅保存 SHA-256 哈希，Passphrase 使用服务器主密钥加密保存，明文只返回一次。
- 首次加密同步后可直接建立后台索引，不再要求用户前往分类管理二次验证。
- 保留旧配置原 Passphrase 解锁和遗失口令后的安全重建流程。
- 检测到旧加密基线时阻止生成随机新密码，避免新配置无法读取旧密文。

## 2026-08-30：Floccus 口令验证诊断与安全重建

- 从真实日志确认 E026 为客户端主动取消，不是服务端解密失败。
- 明确区分 WebDAV 应用密码与 Floccus 加密 Passphrase，修正管理端误导文案。
- 将错误 Passphrase、损坏的密文封装和解密后 XBEL 解析失败拆分为独立错误。
- 加密状态增加保留历史版本的安全基线重建入口，并同步清理服务器旧口令。
- 增加认证失败与 XBEL 解析失败分类回归测试。

## 2026-08-30：Floccus 加密文件后台管理

- 按 Floccus 5.10.2 实际协议实现 PBKDF2-SHA256 与 AES-256-GCM 兼容加解密。
- 使用服务器主密钥加密保存 Floccus passphrase，并明确该模式不再属于零知识加密。
- 加密 WebDAV 上传、临时文件 MOVE、后台编辑、回收站与历史恢复统一维护可重建索引。
- 新增加密状态、解锁和移除口令接口，口令不返回页面且不写日志。
- 完成固定 WebCrypto 兼容向量与加密上传、后台重写仍保持密文的全链路测试。

## 2026-08-30：Floccus 加密转明文安全流程

- 定位直接关闭 Floccus 加密后，客户端把远端密文当作 XBEL 读取并触发 E034。
- 分类管理的加密状态增加安全转换入口，先备份并移除远端加密基线。
- 明确引导用户随后关闭加密并执行一次“向上推一次”，浏览器本地书签不受影响。
- 旧密文继续保留在文件历史版本中，支持后续恢复。

## 2026-08-30：已撤销 Floccus 凭据遗留锁自动接管

- 定位删除旧 Floccus 配置并撤销应用密码后，旧锁继续阻塞新配置的问题。
- 加锁时校验锁所有者对应的应用密码状态，已撤销、过期或不存在时立即清理。
- 新配置无需等待 15 分钟即可接管失效锁，其他有效凭据的活跃锁仍返回 423。
- 新增失效锁接管与活跃锁隔离测试，API 28 项全部通过。

## 2026-08-30：Floccus 并发加锁竞态修复

- 定位新配置强制同步时 5 毫秒内重复加锁触发 E037 的并发竞态。
- 锁标记先完整写入临时文件，再原子发布为正式锁，避免暴露半写入空锁。
- 同一应用密码的并发重入返回创建与继续，不同应用密码仍保持资源锁定。
- 新增并发加锁回归测试并完成 API 27 项全量验证。

## 2026-08-29：Floccus 首次恢复同步加速

- 定位旧 XBEL 缺少节点 ID 导致 Floccus 对 1000 多个节点重复映射和强制比较。
- 新增安全的同步基线快速重建流程，先保留历史版本，再移除旧基线和失效索引。
- 活跃同步锁会阻止重建，操作期间使用独占锁避免与 Floccus 并发写入。
- 重建后由浏览器执行一次“向上推一次”，跳过旧 `NaN` 树的耗时比较。
- 完成 API 26 项测试、管理后台 19 项测试、类型检查和生产构建。

## 2026-08-29：Floccus 强制同步锁重入兼容修复

- 修复 Floccus 遇到旧映射后自动强制重试时，同一应用密码重复加锁被误判为资源冲突的问题。
- 同一锁所有者重复请求现在会刷新锁并继续同步，其他所有者仍返回资源已锁定。
- 新增 WebDAV 锁重入回归测试并完成 API 全量验证。

## 2026-08-29：Floccus XBEL 稳定节点身份兼容修复

- 按 Floccus 5.10.2 的实际格式保存节点数字 ID 和文档 `highestId`。
- 后台移动、删除与恢复不再破坏同步身份，恢复节点使用单调递增的新 ID。
- 缺少完整节点身份的旧 XBEL 暂停后台编辑，引导浏览器安全重建同步身份。
- 完成数据库迁移、解析渲染回归、身份稳定性测试和前后端完整验证。

## 2026-08-29：分类文件夹分组高度一致性修复

- 修复分组标题 sticky 定位造成的滚动后半格空白。
- 收起分组统一为 53px，保留列表表头吸附行为。
- 完成真实数据滚动布局验收、测试和生产构建。

## 2026-08-29：分类管理大数据交互性能优化

- 定位 1017 条书签下分组收起/展开延迟来自整页 React 重渲染。
- 缓存过滤与分组计算，隔离书签分组和右侧组织树渲染。
- 单组交互由约 0.7～1.3 秒降低到约 19～53 毫秒。
- 完成真实 Chromium 性能基准、测试和生产构建。

## 2026-08-29：后台开发服务断连诊断与恢复提示

- 区分真实 API 500、Vite 代理断连和开发服务退出产生的连锁错误。
- 联合开发脚本保留仍正常运行的子服务，并明确打印先退出的服务。
- 管理后台增加 API 断连中文提示与重新连接入口。
- 完成前后端测试、生产构建和真实启动检查，并清理全部诊断资源。

## 2026-08-29：管理后台文件夹分组按钮兼容修复

- 修复 CSS 资源混用时文件夹折叠按钮退化为浏览器原生按钮、表头文字竖排的问题。
- 为分组按钮复用稳定的组织树控件样式，并通过最终覆盖规则锁定表头、分组栏和书签行布局。
- 在 314px 窄列宽度下完成 Chromium 计算样式回归，确认无原生按钮样式和横向溢出。

## 2026-08-29：管理后台书签文件夹分组

- 分类管理左侧书签列表按实际文件夹分组展示，支持根目录与嵌套路径。
- 增加分组折叠、全部展开/收起和整组选择，保留搜索、拖拽与批量移动。
- 完成分组工具单测、管理后台完整测试、生产构建和真实浏览器布局验收。

## 2026-08-28：侧边栏设备码连接

- 后端配对接口新增独立 API 地址和一次性设备码，兑换接口兼容新旧连接格式。
- 管理后台和侧边栏同步更新连接信息展示、输入与帮助文案。
- 完成后端、管理后台和侧边栏的测试与构建验证。

## 2026-08-27：开发端口分离

- 管理后台开发服务使用 `56993` 端口。
- 侧边栏扩展开发服务使用 `56992` 端口。
- 根目录 `pnpm dev` 仅启动 Rust API 与管理后台；侧边栏使用 `pnpm dev:sidebar` 单独启动。

## 2026-08-26：前端 UI/UX 与全局图标系统升级

- 依据设计方案升级管理后台与侧边栏扩展的视觉设计系统，重构设计令牌（统一色彩体系、文字层级、微质感阴影、柔和圆角与平滑过渡）。
- 将管理后台与侧边栏扩展中的手动绘制 SVG 图标全部替换为统一的 `lucide-react` 图标体系，提升图标专业质感与跨端一致性。
- 优化管理后台登录页、概览仪表盘、书签整理器、Floccus 配置向导与账号设置的卡片排版与交互动效。
- 优化侧边栏扩展头部状态指示、快速收藏卡片、书签列表拖拽提示与模态框模糊遮罩。
- 验证双端前端应用编译构建与单元测试，均全部通过。

## 2026-08-26：侧边栏后台授权连接

- 管理后台 Floccus 配置页新增一次性侧边栏连接码，连接码 10 分钟有效且只能兑换一次。
- 侧边栏新增“连接后台”入口，通过连接码兑换受管理后台会话保护的 API 令牌，不重复保存 WebDAV 地址、应用密码、登录密码或 Floccus passphrase。
- 侧边栏保留浏览器原生书签编辑能力，后台连接用于读取真实书签索引和显示连接状态；跨浏览器同步仍由官方 Floccus 负责。
- 新增 `sidebar_pairings` SQLite 迁移，并补充侧边栏 `storage` 与 HTTP/HTTPS 主机访问权限。

## 2026-08-26：保持管理后台登录会话

- 将管理后台会话保存到浏览器本地存储，刷新页面不会丢失登录状态。
- 退出登录时同步清除本地会话；存储异常不会阻止正常登录。

## 2026-08-26：区分加密同步与后台书签索引

- 识别并区分“同步文件已存在但由 Floccus 客户端加密”和“尚未同步”两种状态。
- 管理后台不再把加密同步误报为需要重新配置 Floccus，明确提示书签需在浏览器/Floccus 中管理。
- 明文 XBEL 通过 Floccus 临时文件 MOVE 到最终文件名时，后端会同步建立书签索引。

## 2026-08-26：实现书签整理第一版

- 管理后台「书签整理」接入真实书签索引接口，不再显示占位页。
- 支持按文件夹浏览、标题/网址/路径搜索、打开书签和刷新索引。
- 支持批量选择书签并移动到其他文件夹，使用版本标记避免覆盖并发更新。
- 无同步文件时显示明确的 Floccus 配置引导，保留浏览器端加密同步链路。

## 2026-08-26：修复 Floccus WebDAV MOVE 编码兼容性

- 修复 Floccus 同步最后阶段 `MOVE` 请求返回 400 的问题。
- 对 WebDAV `Destination` 中的百分号编码路径进行逐段解码，兼容邮箱账号中的 `%40`。
- 新增回归测试，覆盖 `05110%40163.com` 到 `05110@163.com` 的目标路径转换。

## 2026-08-26：补齐管理后台账号设置页

- 账号设置页展示注册时填写的登录账号，并明确说明它同时是 WebDAV 用户名。
- 增加可复制的 WebDAV 地址和用户名，提供前往应用密码管理的入口。
- WebDAV 路径中的登录账号使用 URL 编码，兼容邮箱中的 `@` 等特殊字符。

## 2026-08-26：降低默认开发复杂度

- 保留 Rust/Axum、PostgreSQL、WebDAV 和官方 Floccus 作为多浏览器同步链路。
- 默认 `pnpm dev` 只启动 API 和管理后台，避免侧边栏端口占用影响日常开发。
- 侧边栏扩展保留在 workspace 中，通过 `pnpm dev:sidebar` 按需启动。
- 未删除同步代码、数据库迁移或浏览器扩展。

## 2026-08-26：切换 SQLite 存储

- 保留 Bitwarden 式登录、应用密码、WebDAV、Floccus 和文件版本能力。
- 将 SQLx 数据库适配器从 PostgreSQL 切换为 SQLite。
- SQLite 文件默认位于 `data/bookmark-vault.sqlite`，服务器部署不再需要 PostgreSQL 容器。
- 旧 PostgreSQL 数据不会自动迁移；切换前应先备份原数据库和 `DATA_DIR` 文件。

## 2026-08-25

- 阅读并梳理 `DEVELOPMENT_EXECUTION_PLAN.md`。
- 新增 `DEVELOPMENT_PHASED_TASKS.md`，将 MVP 拆分为 P0～P7 八个阶段，并补充任务 ID、依赖关系、交付物、完成门槛和交接模板。
- 当前协同进度：`frontend-sidebar` 和 `backend-hardening` 正在开发；`floccus-qa` 等待侧边栏任务完成后执行。

### 后续衔接

- 由各窗口按任务 ID 认领精确文件范围。
- 侧边栏和后端加固完成后，认领 `integration-qa` 并按 P5 清单完成三浏览器 WebDAV/Floccus 验收。
- 每个阶段验收通过后更新本记录和版本记录。

## 2026-08-25：`release-readiness-audit`

- 汇总后端协议、管理后台 API、侧边栏构建、自部署恢复和 Floccus QA 的现有证据。
- 建立最终发布封板清单，明确当前结论为 `NO-GO / NOT READY`，并记录根 workspace、历史版本恢复、真实浏览器验收、前端界面收敛和商店资料阻塞项。
- 更新分阶段开发任务与执行计划的当前状态，安排历史恢复、前端收敛、workspace 修复、三浏览器验收和商店资料的关闭顺序。
- 未修改业务代码；具体封板步骤见 [`docs/release-readiness.md`](docs/release-readiness.md)。

## 2026-08-25：`final-release-recheck`

- 复核历史版本快照/恢复/清理、前端管理界面和根 pnpm workspace 的完成证据。
- 关闭内部实现阻塞：历史版本恢复已通过 30 条保留策略验证，前端构建和根级 workspace 验证通过。
- 保留真实 Chrome、Edge、Firefox/Floccus 验收和扩展商店正式素材为外部发布阻塞，最终结论仍为 `NO-GO`。
- 更新封板清单、分阶段计划和执行计划；未修改业务代码。
