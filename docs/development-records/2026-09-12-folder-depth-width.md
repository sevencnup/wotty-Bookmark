# 文件夹层级长度开发记录

## 目标

让分类管理页面和侧边栏的文件夹背景条体现真实层级：二级文件夹比一级从左侧收进一段，三级继续收进，以此类推。

## 执行步骤

1. 为分类管理组织树文件夹节点和书签分组标题传递层级变量。
2. 为侧边栏扁平化书签树文件夹行使用现有 `depth` 计算左侧内缩。
3. 保持右侧对齐，让每一级只减少左侧可用长度，不改变文件夹标题和操作按钮的布局逻辑。
4. 设置最小宽度保护，避免深层文件夹在窄侧边栏中不可读。
5. 更新版本记录，执行侧边栏和管理后台验证。
