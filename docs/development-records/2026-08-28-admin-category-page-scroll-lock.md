# 分类管理页面滚动边界修复

## 目标

修复桌面端分类管理页面可以整体滚动、应用壳被移出视口并露出大块空白的问题。

## 执行步骤

1. 固定桌面端根节点和应用壳的视口高度，禁止页面级滚动。
2. 保留主内容区、侧边导航和分类管理内部面板的独立滚动。
3. 在窄屏布局恢复自然页面滚动，避免移动端内容被裁切。
4. 执行类型检查、测试和生产构建，确认样式调整未引入回归。
5. 更新开发记录和版本记录并提交本地仓库。

## 验收标准

- 桌面端分类管理页面无法将整个应用壳上下滑出视口。
- 左侧书签列表和右侧组织树仍可独立滚动。
- 窄屏布局仍能完整浏览两个模块。
- 管理后台类型检查、测试和构建通过。

## 执行结果

- 桌面端根节点和应用壳已固定在可用视口内。
- 移动端自然滚动规则已保留。
- 类型检查、13 项测试和生产构建均通过。
