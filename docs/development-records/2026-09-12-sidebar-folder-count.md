# 侧边栏文件夹总数开发记录

## 目标

在侧边栏文件夹行显示该文件夹及其所有子文件夹中的书签总数，同时避免窄侧边栏因层级缩短而挤压标题。

## 执行步骤

1. 复用书签树递归统计逻辑，为每个文件夹计算包含子文件夹的书签总数。
2. 在文件夹标题右侧增加可读的数量徽章，并提供无障碍标签。
3. 取消侧边栏文件夹背景条随层级缩短的宽度规则，保留行内左侧缩进区分层级。
4. 更新插件版本和版本记录。
5. 执行侧边栏测试、类型检查和生产构建。
