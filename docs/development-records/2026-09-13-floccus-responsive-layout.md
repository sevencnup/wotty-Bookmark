# Floccus 配置向导响应式布局开发记录

## 问题

页面变窄后，WebDAV 配置字段仍保留固定标签宽度和单行值布局，复制按钮被挤出视口，造成横向溢出。

## 处理

1. 在 900px 以下将配置字段改为可收缩的 CSS Grid 布局。
2. 在 560px 以下让字段标签独占一行，值区域允许长地址和密钥换行。
3. 将浏览器商店链接改成小屏单列布局。
4. 更新版本记录并执行管理后台测试、类型检查和生产构建。
