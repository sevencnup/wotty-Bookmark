# 侧边栏回到顶部按钮开发记录

## 目标

在侧边栏书签内容较长、用户已经向下滚动时，提供右下角的快速回到顶部操作。

## 执行步骤

1. 使用侧边栏现有 `.workspace` 滚动容器监听滚动位置。
2. 滚动超过 220px 后显示圆形“回到顶部”按钮，避免页面顶部常驻遮挡内容。
3. 点击按钮通过平滑滚动回到工作区顶部，并在返回顶部后自动隐藏。
4. 增加键盘焦点样式、中文无障碍名称和悬停反馈。
5. 更新侧边栏版本号与版本记录，执行类型检查和构建验证。
