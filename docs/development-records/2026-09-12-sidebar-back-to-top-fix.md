# 侧边栏回到顶部按钮修复开发记录

## 问题

侧边栏点击“回到顶部”后页面位置没有变化。

## 原因

应用没有给根布局和工作区设置明确的高度约束，在部分浏览器侧边栏环境中实际滚动节点可能是工作区以外的文档根节点；按钮只调用工作区的 `scrollTo`，因此没有覆盖真实滚动位置。

## 处理

1. 将根布局固定为视口高度，并允许工作区在弹性布局中收缩后承担滚动。
2. 回顶操作同时重置工作区、文档根节点和窗口滚动位置。
3. 使用即时滚动，避免侧边栏环境对 `behavior: smooth` 支持不一致。
4. 更新版本记录并执行侧边栏测试、类型检查和生产构建。
