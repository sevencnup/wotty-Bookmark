# Floccus 配置字段横向溢出修复开发记录

## 问题

页面变窄后，WebDAV 地址和密钥等长文本字段仍按内容最小宽度布局，导致复制按钮被挤出卡片并产生横向溢出。

## 处理

1. 为配置字段容器、标签和值区域补充 `min-width: 0` 和 `max-width: 100%`。
2. 让配置字段本身裁切超出内容，避免长文本撑大父级卡片。
3. 保留中等宽度三列布局，并在窄屏断点让长值自动换行。
4. 更新版本记录并执行管理后台测试、类型检查和生产构建。
