# 书签库迁移卡片高度开发记录

## 问题

“从 Floccus 同步库迁移”和“导入 XBEL 备份”两个模块的卡片高度不一致，底边和操作按钮无法对齐。

## 处理

1. 将迁移卡片网格设置为拉伸布局，两个卡片共享同一行高度。
2. 卡片内部拆分为标题、可伸展说明区和底部操作区。
3. 将两个操作按钮放在统一的底部基线。
4. 窄屏单列时恢复卡片自然高度，避免产生多余空白。
5. 更新版本记录并执行管理后台测试、类型检查和生产构建。
