# 侧边栏回到顶部按钮可见性修复开发记录

## 问题

回到顶部按钮之前仅在工作区滚动超过 220px 后显示，用户在侧边栏中不容易发现该功能。

## 处理

1. 保留工作区作为唯一滚动容器。
2. 将回到顶部按钮改为右下角常驻显示，避免依赖滚动阈值才能发现。
3. 点击按钮继续使用平滑滚动，将工作区 `scrollTop` 设为 0。
4. 保留键盘焦点、悬停、阴影和无障碍标签。
5. 更新版本记录并执行侧边栏测试、类型检查和生产构建。
