# 侧边栏文件夹视觉对齐开发记录

## 目标

让侧边栏书签树中的文件夹行与管理后台分类管理页面使用一致的蓝色层级背景、文件夹图标卡片和文字层级。

## 执行步骤

1. 为侧边栏文件夹节点增加独立的视觉状态类，不影响普通书签行。
2. 复用分类管理页面的浅蓝渐变背景、边框和悬停状态。
3. 将黄色文件夹图标替换为白色圆角图标卡片中的蓝色文件夹图标。
4. 调整文件夹行高度和标题字重，使其与参考页面的目录行保持一致。
5. 更新版本记录，执行侧边栏测试、类型检查和生产构建。
