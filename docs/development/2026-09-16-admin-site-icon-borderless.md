# 管理后台域名 Logo 去除外框

## 需求

“我的书签库”和“分类管理”页面中的域名 Logo 不再使用带边框、圆角和浅色底的外层图标卡片。

## 方案

- 调整共享的 `.library-site-icon` 样式，保留固定图标槽位，移除外边框、圆角和背景。
- 调整分类管理中的尺寸覆盖样式，确保分类列表不会重新出现图标外框。
- 无图标时的首字母占位同样使用透明背景，保持两页视觉一致。

## 验收

1. 两个页面共用的域名 Logo 外层没有边框或卡片背景。
2. 图标槽位尺寸保持不变，不影响标题和域名排版。
3. 样式回归测试、管理后台测试、类型检查和生产构建通过。
