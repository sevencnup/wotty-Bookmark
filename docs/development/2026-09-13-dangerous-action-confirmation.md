# 危险操作确认设置

## 目标

让管理后台的“危险操作始终确认”偏好统一控制永久删除、清空回收站、撤销设备和移入服务器书签库回收站等操作。

## 执行步骤

1. 提取可复用的偏好读取与危险操作确认辅助逻辑。
2. 接入回收站、设备、服务器书签库、分类管理和导入/恢复等危险操作。
3. 为设置行为补充单元测试，运行前端测试和构建。
4. 更新开发记录与版本记录，并提交本地 Git。

## 验收标准

- 偏好开启时，危险操作显示确认弹窗后才执行。
- 偏好关闭时，危险操作直接执行，不显示确认弹窗。
- 恢复、普通编辑等非危险操作不受影响。
- 刷新页面后偏好仍然有效。
