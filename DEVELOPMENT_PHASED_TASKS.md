# WOTTY BOOKMARK 分阶段开发任务

## 1. 文档目的

本文件根据 [`DEVELOPMENT_EXECUTION_PLAN.md`](DEVELOPMENT_EXECUTION_PLAN.md) 将 MVP 拆分为可认领、可验收、可交接的开发任务。阶段顺序、技术边界和完成门槛以执行计划为准；本文件用于日常排期和进度跟踪。

MVP 的最终目标是：用户可以在管理后台注册并创建 WebDAV 应用密码，使用官方 Floccus 在 Chrome、Edge 和 Firefox 之间同步加密 XBEL，并通过 WOTTY BOOKMARK 侧边栏操作浏览器原生书签。

## 2. 执行规则

- 每个任务开始前必须在 `.ai/coordination/` 中登记唯一的文件范围；未认领成功不得修改文件。
- 每个阶段先完成自身验收门槛，再进入依赖它的阶段。
- 侧边栏只操作浏览器原生书签，不连接 WebDAV，也不保存云端书签副本。
- 服务端只保存 Floccus 加密后的 opaque blob，不解析或记录书签标题、URL 和文件夹名称。
- Node 依赖只在仓库根目录执行 `pnpm install`；禁止在子项目内单独执行 `npm install` 或 `pnpm install`。
- 一次性验收脚本放入 `tests/` 或临时目录，执行后删除，不进入正式产物。
- 每次修改先完成本地验证和提交；未经明确命令不推送 GitHub。
- 阶段完成后更新开发记录和版本记录，并通过协同 `check`、`finish` 释放文件范围。

## 3. 阶段总览

| 阶段 | 任务 ID | 目标 | 依赖 | 当前状态 | 主要交付物 |
| --- | --- | --- | --- | --- | --- |
| P0 | `scope-protocol` | 冻结 MVP 范围和 Floccus/WebDAV 协议边界 | 无 | 已完成 | 架构边界、MVP 验收清单、配置说明初稿 |
| P1 | `repo-foundation` | 建立可测试、可部署的 monorepo 骨架 | P0 | 已完成 | workspace、前后端骨架、Compose、质量检查配置 |
| P2 | `identity-storage` | 完成账户、应用密码和文件元数据存储 | P1 | API 与历史版本恢复已验收 | 认证 API、应用密码、文件版本和审计边界 |
| P3 | `webdav-compatibility` | 让官方 Floccus 稳定使用 WebDAV 服务 | P2 | 协议烟测通过，真实 Floccus 待验收 | WebDAV 方法、锁、临时文件、MOVE、CORS |
| P4 | `admin-console` | 提供账户和 Floccus 配置管理界面 | P2、P3 | API/构建通过，浏览器 E2E 待执行 | 登录、应用密码、配置向导和存储状态页 |
| P5 | `floccus-qa` | 完成三浏览器 WebDAV/Floccus 黑盒验收 | P3、P4、P6 | 环境阻塞 | 可重复验收记录和问题清单 |
| P6 | `sidebar-extension` | 完成三浏览器书签侧边栏体验 | P1、P3 | 代码与构建完成，浏览器回归待执行 | Chrome/Edge/Firefox 生产构建和交互回归 |
| P7 | `release-hardening` | 完成安全加固、自部署和发布准备 | P2、P3、P4、P5、P6 | 历史恢复与 workspace 完成，浏览器/商店发布待完成 | 限流、CORS、备份恢复、部署文档和发布包 |

## 4. 依赖关系

```text
P0 → P1 → P2 → P3 ─┬→ P4 ─┐
                    └→ P6 ─┼→ P5 → P7
                            └──────→ P7
```

P4 与 P6 在 P3 的协议边界稳定后可以并行开发。P5 必须等待侧边栏和管理后台具备可验收版本；P7 的发布封板必须覆盖 P5 的真实浏览器结果。

## 5. 分阶段任务

### P0：冻结范围和协议

目标：锁定首期采用官方 Floccus + WebDAV 的路线，避免重新引入自有同步协议。

任务清单：

- [x] 明确服务器和 Floccus 负责同步，侧边栏只操作浏览器原生书签。
- [x] 明确 Floccus passphrase 由用户自行保管，服务端不托管。
- [x] 明确首期不实现自有同步队列、CRDT、vaultKey 和服务端书签搜索。
- [x] 固定首期支持 Chrome、Edge、Firefox 桌面版。
- [x] 产出架构边界、MVP 验收清单和 Floccus 配置说明初稿。

完成门槛：每个模块都能明确说明是否连接 WebDAV、是否保存书签数据，并与执行计划一致。

### P1：创建工程骨架

目标：建立统一依赖、测试和部署入口。

任务清单：

- [ ] 确认根 `package.json` 和 `pnpm-workspace.yaml` 覆盖所有 Node 项目。
- [ ] 确认 `apps/admin-web`、`apps/sidebar-extension` 和 `packages/protocol` 可独立构建。
- [ ] 确认 Rust API、migration、Docker Compose 和 Caddy 配置可启动。
- [ ] 配置 TypeScript strict、ESLint、Prettier、Vitest、Rustfmt、Clippy 和 Cargo test。
- [ ] 配置本地 CI 检查入口，不推送 GitHub。

完成门槛：在干净环境中，根目录 `pnpm install`、`pnpm lint`、`pnpm test`、`cargo test` 和 `docker compose up` 均可执行；一次性脚本执行后已删除。

### P2：账户、应用密码和文件存储

目标：在 WebDAV 之前建立可靠的身份、授权和存储边界。

任务清单：

- [ ] 验证注册、登录、刷新、退出和当前用户接口。
- [ ] 验证 Argon2id 密码哈希、短期会话和令牌撤销。
- [ ] 验证应用密码只在创建时显示一次，数据库只保存哈希。
- [ ] 验证应用密码撤销后立即失效，账户主密码不能直接访问 WebDAV。
- [ ] 验证用户路径隔离、文件元数据、历史版本和审计日志。
- [ ] 验证日志不包含密码、Authorization 请求头和文件内容。

完成门槛：可以注册、登录、创建/撤销应用密码；不同用户无法访问彼此的文件；数据库中不存在应用密码明文。

### P3：实现 WebDAV 兼容层

目标：使官方 Floccus 能完成读取、锁定、上传、原子替换和释放锁。

任务清单：

- [ ] 验证 `OPTIONS`、`PROPFIND`、`GET`、`HEAD`、`PUT`、`DELETE` 和 `MOVE`。
- [ ] 验证正式文件、临时文件和锁文件路径及认证边界。
- [ ] 验证锁创建、过期、释放、冲突状态码和并发覆盖保护。
- [ ] 验证临时文件通过 `MOVE` 原子替换，上传中断不破坏上一份有效文件。
- [ ] 验证 opaque blob 存储、ETag、Content-Length 和 Last-Modified。
- [ ] 验证 CORS 预检、必要请求头、`Vary: Origin` 和来源白名单。

完成门槛：使用 curl 完成方法测试；官方 Floccus 可以读取空文件并上传加密 XBEL；两个并发客户端不能同时覆盖同一文件。

### P4：开发管理后台

目标：让用户无需手动拼接参数即可得到 Floccus 配置。

任务清单：

- [ ] 完成登录、注册、控制台、应用密码和账户安全页面。
- [ ] 完成 Floccus 配置向导，展示服务地址、用户名、一次性应用密码和文件名。
- [ ] 明确提示 passphrase 由用户自行设置并保存，后台不显示书签树。
- [ ] 展示文件大小、最后同步时间、版本数量和应用密码活动信息。
- [ ] 覆盖网络断开、会话过期、撤销失败和删除确认等错误状态。

完成门槛：新用户可独立完成注册、登录、创建应用密码并得到 Floccus 配置；注册、登录、创建和撤销密码均有端到端测试。

### P5：Floccus 三浏览器集成验收

目标：证明核心同步闭环在真实浏览器中可用。

任务清单：

- [ ] 固定 Chrome、Edge、Firefox 稳定版本和官方 Floccus 测试版本。
- [ ] 使用独立浏览器配置文件验证首次上传、下载和加密 XBEL。
- [ ] 验证书签新增、修改、移动、删除和排序最终收敛。
- [ ] 验证断网修改、恢复联网、并发修改、锁冲突和文件恢复。
- [ ] 撤销应用密码后验证所有 Floccus 请求失败。
- [ ] 记录浏览器、Floccus、服务端版本、结果、证据和遗留问题。

完成门槛：三浏览器均能成功同步；断网修改不丢失；服务端日志和存储中不存在书签明文；新用户可按配置文档完成连接。

### P6：开发书签侧边栏插件

目标：提供书签树、搜索和编辑能力，同时保持“仅操作原生书签”的边界。

任务清单：

- [ ] 验证书签树展示、展开/折叠、搜索和当前目录记忆。
- [ ] 验证新增、编辑、移动、删除、拖拽排序和当前页面收藏。
- [ ] 验证 Chrome/Edge `sidePanel` 和 Firefox `sidebar_action` 构建。
- [ ] 验证原生书签事件刷新和必要权限最小化。
- [ ] 验证侧边栏不连接 WebDAV、不保存云端副本、不维护远端 UUID 映射。
- [ ] 在第二浏览器通过 Floccus 验证侧边栏修改可以收敛。

完成门槛：三浏览器均可打开侧边栏并完成核心操作；构建产物不包含测试脚本、调试日志和开发地址。

### P7：安全、部署和发布

目标：将可用 MVP 收敛为可自部署、可恢复、可分发版本。

任务清单：

- [ ] 完成 CORS 白名单、登录/应用密码限流、安全响应头和请求体限制。
- [ ] 完成 WebDAV 文件大小限制、锁清理、超时控制和非 root 容器运行。
- [ ] 验证 PostgreSQL 与加密文件备份、恢复和历史版本清理策略。
- [ ] 完成干净 VPS 部署、HTTPS 和环境变量文档。
- [ ] 完成隐私政策、应用密码/passphrase 丢失说明和自部署安装文档。
- [ ] 检查 Chrome Web Store、Edge Add-ons 和 Firefox Add-ons 发布包权限及内容。

完成门槛：干净服务器可按文档部署；备份可恢复；三浏览器生产包回归通过；发布包不含一次性测试脚本和调试代码。

## 6. 任务交接模板

每个阶段完成或交接时，记录以下信息：

```text
任务 ID：
负责人：
文件范围：
已完成：
验证命令与结果：
未完成/风险：
依赖的下一任务：
```

## 7. 当前执行顺序

截至 2026-08-25，后续优先级和封板条件见 [`docs/release-readiness.md`](docs/release-readiness.md)：

1. 在具备浏览器和 Floccus 的环境执行 P4/P5 管理后台、侧边栏和三浏览器黑盒验收。
2. 补齐扩展商店图标、截图、正式 Firefox ID、隐私政策 URL 和支持资料。
3. 关闭外部阻塞后重新执行根级测试、生产构建、部署配置和最终发布包审计。

## 8. 当前发布封板状态

截至 2026-08-25，后端协议、管理 API、历史版本恢复、扩展构建、前端界面、根 workspace 和部署恢复已有通过证据；真实浏览器/Floccus验收与商店提交素材仍未完成。当前结论为 `NO-GO`，不得创建正式发布或推送 GitHub。

## 9. 跨窗口交接提示

### 发给 window-2

3 号窗口已完成项目分阶段任务文档。请继续完成 `frontend-sidebar`，重点验证 Chrome、Edge、Firefox 构建与侧边栏功能；完成后释放任务。该任务释放后，3 号窗口将接手依赖它的 `integration-qa`，执行三浏览器 WebDAV/Floccus 验收。

本提示不改变 `frontend-sidebar` 的负责人、文件范围或执行顺序。
